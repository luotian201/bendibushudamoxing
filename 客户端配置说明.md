# 客户端配置说明

本文档说明如何在各种设备和应用中使用 Ollama 服务器。

## 📱 移动设备

### iOS / Android

**方式 1：浏览器直接访问**
```
http://服务器IP:11434
```

**方式 2：使用支持自定义 API 的 App**

推荐 App：
- **ChatBox** (iOS/Android) - 支持自定义 OpenAI API
- **OpenCat** (iOS) - 支持多种 AI 服务

配置方法：
1. 打开 App 设置
2. 添加自定义 API
3. Base URL: `http://服务器IP:11434/v1`
4. 模型: `qwen3:8b`

---

## 💻 桌面电脑

### Windows

**方式 1：设置环境变量**
```powershell
# PowerShell
$env:OLLAMA_HOST="http://服务器IP:11434"

# 或永久设置
[System.Environment]::SetEnvironmentVariable('OLLAMA_HOST', 'http://服务器IP:11434', 'User')
```

**方式 2：每次指定**
```bash
ollama run qwen3:8b --url http://服务器IP:11434 "你好"
```

### macOS / Linux

```bash
# 临时设置
export OLLAMA_HOST=http://服务器IP:11434

# 永久设置（添加到 ~/.bashrc 或 ~/.zshrc）
echo 'export OLLAMA_HOST=http://服务器IP:11434' >> ~/.bashrc
source ~/.bashrc
```

---

## 🛠️ 开发工具集成

### VSCode + Continue

**安装**：
1. 打开 VSCode
2. 安装 Continue 插件

**配置** (`~/.continue/config.json`):
```json
{
  "models": [
    {
      "title": "本地 Qwen3 8B",
      "provider": "ollama",
      "model": "qwen3:8b",
      "apiBase": "http://服务器IP:11434"
    },
    {
      "title": "本地 Gemma3 4B",
      "provider": "ollama",
      "model": "gemma3:4b",
      "apiBase": "http://服务器IP:11434"
    }
  ]
}
```

### Cursor（详细步骤）

**⚠️ 重要：Cursor 需要先配置 API Provider，才能使用模型！**

#### 步骤 1：打开设置
- 方法 1：点击 Cursor 右上角 **齿轮图标** ⚙️
- 方法 2：快捷键 `Ctrl + ,` (Windows) 或 `Cmd + ,` (Mac)

#### 步骤 2：进入 Models 配置
1. 在设置页面，点击左侧 **Models** 选项
2. 找到 **Model Providers** 或 **OpenAI API** 区域

#### 步骤 3：添加 OpenAI-Compatible Provider
1. 点击 **Add OpenAI-Compatible Provider** 或 **Add Custom Model**
2. 填写配置：

```
Provider Name: Ollama 本地
Base URL: http://服务器IP:11434/v1
API Key: ollama （随便填，必须填写）
```

**📌 关键点：Base URL 必须以 `/v1` 结尾！**

#### 步骤 4：选择模型
配置完 Provider 后：
1. 在 **Chat Model** 或 **Model** 下拉菜单中
2. 选择或手动输入：`qwen3:8b`
3. 如果下拉菜单没有，直接输入模型名称即可

#### 步骤 5：验证配置
1. 打开 Cursor 的 AI Chat 面板
2. 发送一条消息测试
3. 如果成功回复，说明配置完成！

#### 🔧 常见问题

**Q1: 找不到 "Add Model" 按钮？**
- A: 新版 Cursor 改名了，找 **Add OpenAI-Compatible Provider** 或 **Override OpenAI Base URL**

**Q2: 添加后没有模型可选？**
- A: 直接在模型输入框中手动输入 `qwen3:8b`，不要等下拉菜单

**Q3: 显示 "Connection Error"？**
- A: 检查 IP 地址是否正确，确保 `/v1` 后缀存在

**Q4: 模型列表是空的？**
- A: 这是正常的！Ollama 的模型列表需要手动输入模型名称

#### 💡 快速配置示例

如果服务器 IP 是 `192.168.1.100`，配置如下：

```
Base URL: http://192.168.1.100:11434/v1
API Key: ollama
Model: qwen3:8b
```

#### 🎯 配置完成后的使用

- **AI Chat**: 直接聊天
- **Code Completion**: 自动补全代码
- **Cmd+K**: 选中代码后按 Cmd+K 进行编辑

### JetBrains IDEs (PyCharm, IntelliJ, etc.)

**使用 AI Assistant 插件**：
1. 安装 **AI Assistant** 插件
2. 设置 → AI Assistant → Custom Provider
3. 配置：
   - Provider URL: `http://服务器IP:11434/v1`
   - Model: `qwen3:8b`

---

## 🐍 Python 客户端

### 方式 1：使用 requests

```python
import requests

OLLAMA_URL = "http://服务器IP:11434"

response = requests.post(
    f"{OLLAMA_URL}/api/generate",
    json={
        "model": "qwen3:8b",
        "prompt": "你好",
        "stream": False
    }
)

print(response.json()["response"])
```

### 方式 2：使用 OpenAI SDK

```python
from openai import OpenAI

client = OpenAI(
    base_url="http://服务器IP:11434/v1",
    api_key="ollama"  # 随意填写
)

response = client.chat.completions.create(
    model="qwen3:8b",
    messages=[
        {"role": "user", "content": "你好"}
    ]
)

print(response.choices[0].message.content)
```

### 方式 3：使用 LangChain

```python
from langchain_community.llms import Ollama

llm = Ollama(
    base_url="http://服务器IP:11434",
    model="qwen3:8b"
)

response = llm.invoke("用 Python 写一个快速排序")
print(response)
```

---

## 🌐 JavaScript / TypeScript

### Node.js

```javascript
const axios = require('axios');

const OLLAMA_URL = 'http://服务器IP:11434';

async function chat(prompt) {
  const response = await axios.post(`${OLLAMA_URL}/api/generate`, {
    model: 'qwen3:8b',
    prompt: prompt,
    stream: false
  });

  return response.data.response;
}

chat('你好，介绍一下你自己').then(console.log);
```

### 浏览器

```javascript
async function chat(prompt) {
  const response = await fetch('http://服务器IP:11434/api/generate', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      model: 'qwen3:8b',
      prompt: prompt,
      stream: false
    })
  });

  const data = await response.json();
  return data.response;
}
```

---

## 🧪 测试工具

### cURL

**基本测试**：
```bash
curl http://服务器IP:11434
```

**获取模型列表**：
```bash
curl http://服务器IP:11434/api/tags
```

**发送请求**：
```bash
curl http://服务器IP:11434/api/generate -d '{
  "model": "qwen3:8b",
  "prompt": "你好",
  "stream": false
}'
```

### Postman / Insomnia

**请求配置**：
- Method: `POST`
- URL: `http://服务器IP:11434/api/generate`
- Headers: `Content-Type: application/json`
- Body:
```json
{
  "model": "qwen3:8b",
  "prompt": "你好",
  "stream": false
}
```

---

## 🔧 故障排查

### 连接失败

1. **检查网络**：
   ```bash
   ping 服务器IP
   ```

2. **检查端口**：
   ```bash
   telnet 服务器IP 11434
   ```

3. **检查防火墙**：
   - 确认服务器防火墙允许 11434 端口
   - 确认客户端防火墙允许出站连接

### 请求超时

1. **增加超时时间**：
   ```python
   response = requests.post(url, json=payload, timeout=60)
   ```

2. **检查模型是否已加载**：
   ```bash
   curl http://服务器IP:11434/api/ps
   ```

---

## 💡 最佳实践

1. **使用环境变量**：避免硬编码 IP 地址
2. **错误处理**：添加 try-except 捕获网络错误
3. **连接池**：高频调用时使用连接池
4. **异步请求**：大量请求时使用异步库
5. **缓存结果**：相同问题缓存结果避免重复请求

---

## 📚 更多资源

- [Ollama API 文档](https://github.com/ollama/ollama/blob/main/docs/api.md)
- [OpenAI API 兼容](https://github.com/ollama/ollama/blob/main/docs/openai.md)
- [模型库](https://ollama.com/library)

---

**最后更新**: 2026-03-11

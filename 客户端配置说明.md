# 客户端配置说明

本文档说明如何在各种设备和应用中使用 Ollama 服务器。

## 📱 移动设备

### iOS / Android

**方式 1：浏览器直接访问**
```
http://服务器IP:11434
```

**方式 2：使用支持自定义 API 的 App**

推荐 App：
- **ChatBox** (iOS/Android) - 支持自定义 OpenAI API
- **OpenCat** (iOS) - 支持多种 AI 服务

配置方法：
1. 打开 App 设置
2. 添加自定义 API
3. Base URL: `http://服务器IP:11434/v1`
4. 模型: `qwen3:8b`

---

## 💻 桌面电脑

### Windows

**方式 1：设置环境变量**
```powershell
# PowerShell
$env:OLLAMA_HOST="http://服务器IP:11434"

# 或永久设置
[System.Environment]::SetEnvironmentVariable('OLLAMA_HOST', 'http://服务器IP:11434', 'User')
```

**方式 2：每次指定**
```bash
ollama run qwen3:8b --url http://服务器IP:11434 "你好"
```

### macOS / Linux

```bash
# 临时设置
export OLLAMA_HOST=http://服务器IP:11434

# 永久设置（添加到 ~/.bashrc 或 ~/.zshrc）
echo 'export OLLAMA_HOST=http://服务器IP:11434' >> ~/.bashrc
source ~/.bashrc
```

---

## 🛠️ 开发工具集成

### VSCode + Continue

**安装**：
1. 打开 VSCode
2. 安装 Continue 插件

**配置** (`~/.continue/config.json`):
```json
{
  "models": [
    {
      "title": "本地 Qwen3 8B",
      "provider": "ollama",
      "model": "qwen3:8b",
      "apiBase": "http://服务器IP:11434"
    },
    {
      "title": "本地 Gemma3 4B",
      "provider": "ollama",
      "model": "gemma3:4b",
      "apiBase": "http://服务器IP:11434"
    }
  ]
}
```

### Cursor

1. 打开 Cursor 设置
2. 选择 **Models** → **Add Model**
3. 选择 **OpenAI API**
4. 配置：
   - API Key: 留空或随意填写
   - Base URL: `http://服务器IP:11434/v1`
   - Model: `qwen3:8b`

### JetBrains IDEs (PyCharm, IntelliJ, etc.)

**使用 AI Assistant 插件**：
1. 安装 **AI Assistant** 插件
2. 设置 → AI Assistant → Custom Provider
3. 配置：
   - Provider URL: `http://服务器IP:11434/v1`
   - Model: `qwen3:8b`

---

## 🐍 Python 客户端

### 方式 1：使用 requests

```python
import requests

OLLAMA_URL = "http://服务器IP:11434"

response = requests.post(
    f"{OLLAMA_URL}/api/generate",
    json={
        "model": "qwen3:8b",
        "prompt": "你好",
        "stream": False
    }
)

print(response.json()["response"])
```

### 方式 2：使用 OpenAI SDK

```python
from openai import OpenAI

client = OpenAI(
    base_url="http://服务器IP:11434/v1",
    api_key="ollama"  # 随意填写
)

response = client.chat.completions.create(
    model="qwen3:8b",
    messages=[
        {"role": "user", "content": "你好"}
    ]
)

print(response.choices[0].message.content)
```

### 方式 3：使用 LangChain

```python
from langchain_community.llms import Ollama

llm = Ollama(
    base_url="http://服务器IP:11434",
    model="qwen3:8b"
)

response = llm.invoke("用 Python 写一个快速排序")
print(response)
```

---

## 🌐 JavaScript / TypeScript

### Node.js

```javascript
const axios = require('axios');

const OLLAMA_URL = 'http://服务器IP:11434';

async function chat(prompt) {
  const response = await axios.post(`${OLLAMA_URL}/api/generate`, {
    model: 'qwen3:8b',
    prompt: prompt,
    stream: false
  });

  return response.data.response;
}

chat('你好，介绍一下你自己').then(console.log);
```

### 浏览器

```javascript
async function chat(prompt) {
  const response = await fetch('http://服务器IP:11434/api/generate', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      model: 'qwen3:8b',
      prompt: prompt,
      stream: false
    })
  });

  const data = await response.json();
  return data.response;
}
```

---

## 🧪 测试工具

### cURL

**基本测试**：
```bash
curl http://服务器IP:11434
```

**获取模型列表**：
```bash
curl http://服务器IP:11434/api/tags
```

**发送请求**：
```bash
curl http://服务器IP:11434/api/generate -d '{
  "model": "qwen3:8b",
  "prompt": "你好",
  "stream": false
}'
```

### Postman / Insomnia

**请求配置**：
- Method: `POST`
- URL: `http://服务器IP:11434/api/generate`
- Headers: `Content-Type: application/json`
- Body:
```json
{
  "model": "qwen3:8b",
  "prompt": "你好",
  "stream": false
}
```

---

## 🔧 故障排查

### 连接失败

1. **检查网络**：
   ```bash
   ping 服务器IP
   ```

2. **检查端口**：
   ```bash
   telnet 服务器IP 11434
   ```

3. **检查防火墙**：
   - 确认服务器防火墙允许 11434 端口
   - 确认客户端防火墙允许出站连接

### 请求超时

1. **增加超时时间**：
   ```python
   response = requests.post(url, json=payload, timeout=60)
   ```

2. **检查模型是否已加载**：
   ```bash
   curl http://服务器IP:11434/api/ps
   ```

---

## 💡 最佳实践

1. **使用环境变量**：避免硬编码 IP 地址
2. **错误处理**：添加 try-except 捕获网络错误
3. **连接池**：高频调用时使用连接池
4. **异步请求**：大量请求时使用异步库
5. **缓存结果**：相同问题缓存结果避免重复请求

---

## 📚 更多资源

- [Ollama API 文档](https://github.com/ollama/ollama/blob/main/docs/api.md)
- [OpenAI API 兼容](https://github.com/ollama/ollama/blob/main/docs/openai.md)
- [模型库](https://ollama.com/library)

---

**最后更新**: 2026-03-11

# 快速开始指南

**5 分钟快速部署 Ollama 本地大模型**

## 🎯 目标

完成后你将拥有：
- ✅ 一个运行在本地的大模型服务器
- ✅ 可以从局域网内任何设备访问
- ✅ 支持多种编程语言调用

---

## 📝 前提条件

- Windows 10/11 操作系统
- NVIDIA 显卡（推荐 6GB+ 显存）
- 稳定的网络连接

---

## 🚀 三步部署

### 步骤 1：安装 Ollama（2 分钟）

1. 访问 https://ollama.com
2. 点击 "Download for Windows"
3. 运行安装程序
4. 安装完成后，Ollama 会自动启动

**验证安装**：
```bash
ollama --version
```

---

### 步骤 2：下载模型（2 分钟）

**方式 1：使用 GUI（推荐）**
1. 打开 Ollama 窗口
2. 在模型列表中选择 `qwen3:8b`
3. 发送一条消息，自动开始下载

**方式 2：使用命令行**
```bash
ollama pull qwen3:8b
```

**等待下载完成**（约 5.2 GB）

---

### 步骤 3：配置局域网访问（1 分钟）

**以管理员身份运行** `配置局域网访问.bat`

或手动执行：
```powershell
# 1. 设置环境变量
[System.Environment]::SetEnvironmentVariable('OLLAMA_HOST', '0.0.0.0:11434', 'User')

# 2. 添加防火墙规则
netsh advfirewall firewall add rule name="Ollama Server" dir=in action=allow protocol=TCP localport=11434
```

**重启 Ollama**：
- 右键托盘图标 → Quit
- 重新打开 Ollama

---

## ✅ 测试部署

### 本机测试

```bash
curl http://localhost:11434
# 返回: Ollama is running
```

### 局域网测试

1. **获取本机 IP**：
   ```bash
   ipconfig
   ```
   记下 IPv4 地址（如 `10.0.0.184`）

2. **在手机/其他设备访问**：
   ```
   http://你的IP:11434
   ```
   应该看到 "Ollama is running"

3. **测试 API**：
   ```bash
   curl http://你的IP:11434/api/tags
   ```
   返回模型列表

---

## 🎉 开始使用

### 命令行聊天

```bash
ollama run qwen3:8b
```

### Python 调用

```python
import requests

response = requests.post(
    "http://localhost:11434/api/generate",
    json={
        "model": "qwen3:8b",
        "prompt": "你好",
        "stream": False
    }
)

print(response.json()["response"])
```

### 浏览器测试

访问：
```
http://localhost:11434/api/tags
```

---

## 📊 推荐模型

| 模型 | 大小 | 用途 | 下载命令 |
|------|------|------|----------|
| qwen3:8b | 5.2 GB | 中文对话 | `ollama pull qwen3:8b` |
| gemma3:4b | 3.3 GB | 快速响应 | `ollama pull gemma3:4b` |
| deepseek-r1:7b | 4.5 GB | 代码/推理 | `ollama pull deepseek-r1:7b` |

---

## ❓ 遇到问题？

### 无法下载模型？
- 检查网络连接
- 尝试重启 Ollama
- 先下载小模型测试（`gemma3:4b`）

### 其他设备无法访问？
- 确认设备在同一 WiFi
- 检查防火墙规则
- 使用 IP 地址而不是 localhost

### 显存不足？
- RTX 2060 6GB 推荐使用 `qwen3:8b` 或更小的模型
- 避免同时运行多个模型

---

## 📚 下一步

- 查看 [客户端配置说明.md](./客户端配置说明.md) 了解更多集成方式
- 查看 [README.md](./README.md) 获取完整文档
- 查看 [Python调用示例.py](./Python调用示例.py) 学习编程调用

---

**遇到问题？** 查看 [常见问题](#) 或提交 Issue

**最后更新**: 2026-03-11
